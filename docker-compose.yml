services:
  app:
    command: >
      sh -c "echo '[dev] Clearing stale Next.js cache...' && rm -rf apps/web/.next && pnpm dev"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT:-3001}:3000"
    environment:
      - REDIS_URL=redis://redis:6379
      - DATA_DIR=/data
      - TURBO_CACHE_DIR=/tmp/.turbo
      - NODE_ENV=development
      - HOME=/tmp
      - COREPACK_HOME=/tmp/corepack
    volumes:
      - .:/app
      - /app/node_modules
      - app_data:/data
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://localhost:3000/api/health').then(r=>{process.exit(r.ok?0:1)}).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 45s
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

volumes:
  app_data:
  redis_data:
